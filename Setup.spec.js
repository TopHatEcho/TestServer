const assert = require('assert');
const Setup = require('./Setup.js');



describe('Setup Function', () => {

  it('should post to console', () => {
    const result = Setup.Setup();
    assert.equal(result , );
  });






});
















